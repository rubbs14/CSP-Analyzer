<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CspAnalyzer.Desktop.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="1400" d:DesignHeight="820"
        x:Class="CspAnalyzer.Desktop.Views.MainWindow"
        x:DataType="vm:MainViewModel"
        Icon="/Assets/avalonia-logo.ico"
        Title="CSP Analyzer"
        Width="1400" Height="820" MinWidth="1000" MinHeight="650">

    <!--
    Layout shell only (S7) - empty placeholder regions mirroring
    CSPv2/Form1.Designer.cs's panel layout (see the S7 wireframe survey in
    docs/superpowers/SESSIONS.md history / session memory). No bindings or
    real charts yet - that's S8 (dataset loading), S9 (run flow), S10
    (results view/charts).
    -->
    <Design.DataContext>
        <vm:MainViewModel />
    </Design.DataContext>

    <Window.Styles>
        <Style Selector="Border.section">
            <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundBaseLowBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="4" />
        </Style>
        <Style Selector="TextBlock.sectionHeader">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,0,0,4" />
        </Style>
        <Style Selector="TextBlock.placeholder">
            <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
            <Setter Property="FontStyle" Value="Italic" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Styles>

    <!-- Outer: fixed-width sidebar (Form1's tableLayoutPanel1, ~204px) + fill content (tableLayoutPanel2) -->
    <Grid ColumnDefinitions="210,*">

        <!-- Sidebar: panel1 / panel4 / panel3 / panel13 -->
        <Grid Grid.Column="0" RowDefinitions="Auto,*,Auto,Auto">
            <Border Grid.Row="0" Classes="section">
                <StackPanel>
                    <TextBlock Classes="sectionHeader" Text="File I/O" />
                    <Button Content="Load Reference" HorizontalAlignment="Stretch" Margin="0,2" />
                    <Button Content="Load Dataset" HorizontalAlignment="Stretch" Margin="0,2" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Classes="section">
                <StackPanel>
                    <TextBlock Classes="sectionHeader" Text="Import Controls / Run" />
                    <TextBlock Classes="placeholder" Text="15N/1H range, intensity thresholds, dataset stats, Run CSP (S8/S9)" TextWrapping="Wrap" />
                    <Button Content="Run CSP" HorizontalAlignment="Stretch" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Classes="section">
                <StackPanel>
                    <TextBlock Classes="sectionHeader" Text="Run Info" />
                    <TextBlock Classes="placeholder" Text="Status / progress bar (S9)" />
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Classes="section">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
                    <Button Content="Help" />
                    <Button Content="Details" />
                    <Button Content="Shortcuts" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Content: chart zone (tableLayoutPanel3) + bottom bar (tableLayoutPanel6) -->
        <Grid Grid.Column="1" RowDefinitions="*,130">

            <!-- Chart zone: left stacked bar charts + right spectra overlay -->
            <Grid Grid.Row="0" ColumnDefinitions="*,*">
                <Grid Grid.Column="0" RowDefinitions="*,*">
                    <Border Grid.Row="0" Classes="section">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Peaks Difference Distribution" />
                            <TextBlock Classes="placeholder" Text="Peak-difference chart (S10)" />
                        </DockPanel>
                    </Border>
                    <Border Grid.Row="1" Classes="section">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Probability Distribution" />
                            <TextBlock Classes="placeholder" Text="Probability chart (S10)" />
                        </DockPanel>
                    </Border>
                </Grid>

                <Border Grid.Column="1" Classes="section">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Spectra Overlay" />
                        <TextBlock Classes="placeholder" Text="Spectra overlay scatter chart, zoom + mark-active/inactive controls (S9/S10)" TextWrapping="Wrap" />
                    </DockPanel>
                </Border>
            </Grid>

            <!-- Bottom bar: stats/results strip + experiment-navigation console -->
            <Grid Grid.Row="1" ColumnDefinitions="*,*">

                <Grid Grid.Column="0" ColumnDefinitions="*,*,*">
                    <Border Grid.Column="0" Classes="section">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Analysis Info" />
                            <TextBlock Classes="placeholder" Text="Reference / dataset stats (S8)" TextWrapping="Wrap" />
                        </DockPanel>
                    </Border>
                    <Border Grid.Column="1" Classes="section">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Automated Analysis Results" />
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                                <CheckBox Content="Actives" />
                                <CheckBox Content="Inactives" />
                            </StackPanel>
                            <TextBlock Classes="placeholder" Text="Actives/inactives gauges (S10)" />
                        </DockPanel>
                    </Border>
                    <Border Grid.Column="2" Classes="section">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" Classes="sectionHeader" Text="Manual Analysis Results" />
                            <TextBlock Classes="placeholder" Text="Manual-results chart + reset-flags (S10)" TextWrapping="Wrap" />
                        </DockPanel>
                    </Border>
                </Grid>

                <Grid Grid.Column="1" RowDefinitions="Auto,*">
                    <Grid Grid.Row="0" ColumnDefinitions="*,*">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="4" Spacing="4">
                            <TextBlock Text="Current Experiment:" VerticalAlignment="Center" />
                            <TextBlock Text="- / -" VerticalAlignment="Center" />
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="4" Spacing="4">
                            <TextBlock Text="Go To Experiment:" VerticalAlignment="Center" />
                            <TextBox Width="60" />
                        </StackPanel>
                    </Grid>
                    <Grid Grid.Row="1" ColumnDefinitions="*,*">
                        <Grid Grid.Column="0" ColumnDefinitions="*,*,*,*" Margin="4">
                            <Button Grid.Column="0" Content="|&lt;" HorizontalAlignment="Stretch" />
                            <Button Grid.Column="1" Content="&lt;" HorizontalAlignment="Stretch" />
                            <Button Grid.Column="2" Content="&gt;" HorizontalAlignment="Stretch" />
                            <Button Grid.Column="3" Content="&gt;|" HorizontalAlignment="Stretch" />
                        </Grid>
                        <StackPanel Grid.Column="1" Margin="4" Spacing="2">
                            <TextBlock Classes="placeholder" Text="Man./auto status, ΔPeaks (S9)" TextWrapping="Wrap" FontSize="10" />
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <Button Content="Export" />
                                <Button Content="Reset" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
